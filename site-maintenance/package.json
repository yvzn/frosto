{
	"name": "frosto-site-maintenance",
	"version": "1.0.0",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"postbuild": "purgecss --content dist/*.html dist/assets/*.js --css dist/assets/*.css --output dist/assets/"
	},
	"devDependencies": {
		"purgecss": "^5.0.0",
		"vite": "^4.2.0",
		"vite-plugin-dotenv-in-html": "^4.0.0"
	},
	"dependencies": {
		"bootstrap": "^5.2.1"
	}
}
