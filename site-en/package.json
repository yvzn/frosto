{
	"name": "frosto-site-en",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"postbuild": "purgecss --content dist/*.html dist/assets/*.js --css dist/assets/*.css --output dist/assets/"
	},
	"devDependencies": {
		"purgecss": "^7.0.2",
		"vite": "^7.1.3"
	},
	"dependencies": {
		"bootstrap": "^5.3.7"
	}
}
